import 'package:flutter/material.dart';

class ResidentMaintenanceScreen extends StatelessWidget {
  const ResidentMaintenanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('維修申請'),
      ),
      body: const Center(
        child: Text('維修功能開發中...'),
      ),
    );
  }
} 